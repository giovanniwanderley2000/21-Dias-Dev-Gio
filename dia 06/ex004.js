//Algoritmo para exibir a tabuada de um número e dos próximos 2 números
let numeroRecebido = Number(prompt("Digite um número"))
let numeroSeguinte1 = numeroRecebido + 1
let numeroSeguinte2 = numeroRecebido + 2


for (numeroRecebido = numeroRecebido; numeroRecebido <= numeroRecebido + 2; numeroRecebido++) {
    console.log(numeroRecebido + " x " + i + " = " + numeroRecebido * i)
}

for (let i = 0; i <= 10; i++) {

    console.log(numeroRecebido + " x " + i + " = " + numeroRecebido * i)
}
